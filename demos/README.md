# Geometry Blender Demos (Jupyter Notebooks)

To execute the code in the Jupyter Notebooks you will need to install 
the plotly graphing library as follows:

- Anaconda Python: *conda install -c plotly plotly*